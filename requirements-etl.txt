# ETL-only dependencies. Used by scripts/ that build the data (community
# detection, item loading, variant generation, summary generation, etc.).
# The deployed Streamlit app does NOT need these — see requirements.txt.
-r requirements.txt
graspologic>=3.3.0
networkx>=3.0
boto3>=1.34.0
google-genai>=1.0.0
pandas>=2.0.0
rapidfuzz>=3.0.0
